EXTRA_DIST = autogen.sh COPYING INSTALL README AUTHORS THANKS NEWS glusterfs.spec

SUBDIRS = libglusterfs xlators scheduler transport $(GLUSTERFSD_SUBDIR) $(FUSE_CLIENT_SUBDIR) glusterfs-booster doc extras 


CLEANFILES = *~