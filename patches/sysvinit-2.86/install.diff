diff -pruN sysvinit-2.86/src/Makefile sysvinit-2.86-h/src/Makefile
--- sysvinit-2.86/src/Makefile	2004-06-09 18:17:45.000000000 +0530
+++ sysvinit-2.86-h/src/Makefile	2006-01-22 10:04:15.000000000 +0530
@@ -8,7 +8,7 @@
 # Version:	@(#)Makefile  2.85-13  23-Mar-2004  miquels@cistron.nl
 #
 
-CC	= gcc
+CC	= $(CROSS)gcc
 CFLAGS	= -Wall -O2 -fomit-frame-pointer -D_GNU_SOURCE
 LDFLAGS	= -s
 STATIC	=
@@ -46,7 +46,7 @@ endif
 BIN_OWNER	= root
 BIN_GROUP	= root
 BIN_COMBO	= $(BIN_OWNER):$(BIN_GROUP)
-INSTALL		= install -o $(BIN_OWNER) -g $(BIN_GROUP)
+INSTALL		= install -D
 MANDIR		= /usr/share/man
 
 # Additional libs for GNU libc.
@@ -111,13 +111,13 @@ distclean:	clobber
 
 install:
 		for i in $(BIN); do \
-			$(INSTALL) -m 755 $$i $(ROOT)/bin/; \
+			$(INSTALL) -m 755 $$i $(ROOT)/bin/$$i; \
 		done
 		for i in $(SBIN); do \
-			$(INSTALL) -m 755 $$i $(ROOT)/sbin/; \
+			$(INSTALL) -m 755 $$i $(ROOT)/sbin/$$i; \
 		done
 		for i in $(USRBIN); do \
-			$(INSTALL) -m 755 $$i $(ROOT)/usr/bin/; \
+			$(INSTALL) -m 755 $$i $(ROOT)/usr/bin/$$i; \
 		done
 		# $(INSTALL) -m 755 etc/initscript.sample $(ROOT)/etc/
 		ln -sf halt $(ROOT)/sbin/reboot
@@ -127,15 +127,15 @@ install:
 		if [ ! -f $(ROOT)/usr/bin/lastb ]; then \
 			ln -sf last $(ROOT)/usr/bin/lastb; \
 		fi
-		$(INSTALL) -m 644 initreq.h $(ROOT)/usr/include/
+		$(INSTALL) -m 644 initreq.h $(ROOT)/usr/include/initreq.h
 		for i in $(MAN1); do \
-			$(INSTALL) -m 644 ../man/$$i $(ROOT)$(MANDIR)/man1/; \
+			$(INSTALL) -m 644 ../man/$$i $(ROOT)$(MANDIR)/man1/$$i; \
 		done
 		for i in $(MAN5); do \
-			$(INSTALL) -m 644 ../man/$$i $(ROOT)$(MANDIR)/man5/; \
+			$(INSTALL) -m 644 ../man/$$i $(ROOT)$(MANDIR)/man5/$$i; \
 		done
 		for i in $(MAN8); do \
-			$(INSTALL) -m 644 ../man/$$i $(ROOT)$(MANDIR)/man8/; \
+			$(INSTALL) -m 644 ../man/$$i $(ROOT)$(MANDIR)/man8/$$i; \
 		done
 ifeq ($(ROOT),)
 		#
