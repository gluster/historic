diff -pru parted-1.6.25_orig/parted/parted.c parted-1.6.25/parted/parted.c
--- parted-1.6.25_orig/parted/parted.c	2005-11-02 16:49:45.000000000 +0530
+++ parted-1.6.25/parted/parted.c	2006-06-05 13:20:24.000000000 +0530
@@ -888,6 +888,8 @@ partition_print_flags (PedPartition* par
 			printf (_(ped_partition_flag_get_name (flag)));
 		}
 	}
+	if (first_flag)
+	        printf ("*");
 }
 
 /* Prints a sector out, first in compact form, and then with a percentage.
@@ -1041,7 +1043,7 @@ do_print (PedDevice** dev)
 			printf ("%-9s ",
 				_(ped_partition_type_get_name (part->type)));
 
-		printf ("%-12s ", part->fs_type ? part->fs_type->name : "");
+		printf ("%-12s ", part->fs_type ? part->fs_type->name : "-");
 
 		if (has_name)
 			printf ("%-21s ", ped_partition_get_name (part));
